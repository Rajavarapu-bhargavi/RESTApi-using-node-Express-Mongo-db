# RESTApi-using-node-Express-Mongo-db
RESTful API using Node, Express, and MongoDB. It will create endpoints for creating data, reading data, updating data, and deleting data (basic CRUD operations).
